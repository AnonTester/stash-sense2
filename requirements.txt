# Core
python-dotenv>=1.0.0,<2
requests>=2.28.0,<3
tqdm>=4.65.0,<5

# ML - PyTorch with CUDA 12.8 (for Blackwell GPUs)
# Only used in dev for model conversion (convert_models_to_onnx.py) and
# tattoo detection; the Docker image uses ONNX Runtime at runtime instead.
# deepface (TensorFlow-based FaceNet512/ArcFace conversion) was removed
# with the buffalo_l migration -- no longer needed for anything.
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch>=2.5.0
torchvision>=0.20.0
open-clip-torch>=2.24.0  # CLIP ViT-B/32 conversion for tattoo embedding

# Face recognition
insightface>=0.7.3,<1  # buffalo_l detection+recognition (GPU via ONNX Runtime)
onnxruntime-gpu>=1.15.0,<2  # GPU support for InsightFace
usearch>=2.16.0,<3

# Body pose estimation
mediapipe>=0.10.9

# API (for sidecar)
fastapi>=0.100.0,<1
uvicorn[standard]>=0.23.0,<1
httpx>=0.25.0,<1

# Image processing
pillow>=12.1.1,<13
numpy>=1.24,<2
opencv-python-headless>=4.8.0,<5

# Utilities
pydantic>=2.0.0,<3

# Model downloads
gdown>=4.7.0,<6
